{
  "version": 2,
  "routes": [
    { "src": "/traffic-signal", "dest": "./index.py" }
    // Add more routes as needed
  ],
  "builds": [
    { "src": "server/**/*.py", "use": "@vercel/python" }
  ]
}

